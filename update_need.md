need updates:
- is this project production ready?
- does this site handles large amount of users my queue and concurrancy controll
- each can upload mutiple image upto 5 only at a time and single image can only be processed beacause the server craches the system if mutiple are processed at time like my vscode craches
- when i upload a image it is going as 2 images to worker because of gloable drop ,zone drop . i guess we don't need zone drop if have gloable drop can catch it
- after completing all queue of user notify user with toster
- make UI/UX more morden easy to access using shadcn components
- i saw a reference site where each sections: top is header with navbar home, hero with drag & drop area inside chose file , middle preview of processed image with slider as we did after that all uploaded original images as row small 100x100px i guess on seleting a image the preview shows seleted one with how seconds to get result,resolution,slider scroll left processed image, right original image and some other helping tools like eraser/redo, at last section some matter with feed back   